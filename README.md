# Video Stabilization using Open-CV

## Data

Install requirements using `python install -r requirements.txt`.

Run `download_data.py` to download the DeepStab dataset.

The dataset will be downloaded to `data/DeepStab`.